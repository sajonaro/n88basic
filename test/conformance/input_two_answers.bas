10 REM IN.INPUT: two separate INPUT statements each read their own line,
20 REM which is unchanged by the rule that ONE statement's several
30 REM variables share ONE line (see input_one_line_fields).
40 INPUT A
50 INPUT B$
60 PRINT A; B$
70 END
