10 REM STR.CHR / STR.VAL: implemented in code, but their clause bodies
20 REM are still spec-stub placeholders (clauses.json status "absent")
30 REM as of spec 0.1.0 -- this case exercises the behaviour anyway, as
40 REM evidence ready for whoever writes those clauses next.
50 PRINT CHR$(65)
60 PRINT VAL("12.5")
70 PRINT VAL("XY")
80 END
